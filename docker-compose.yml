version: '3.8'

services:
  web:
    container_name: web-rizqi
    build: 
      context: ./portfolio-nextjs
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    # volumes: tidak diperlukan karena file sudah dibake ke dalam image
